# AutoLink Ghana Website
Open `index.html` in a browser to preview the site.

## GitHub Pages
Upload `index.html` to the root of your GitHub repository, commit the change, then enable:
Settings -> Pages -> Deploy from a branch -> main -> / (root) -> Save.

This is a website preview. The buttons for listings, accounts, payments, and live seller databases are placeholders until those systems are built.
